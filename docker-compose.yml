## docker-compose.yml

version: '3'

services:
  basedevel:
    build:
      context: docker
      dockerfile: Dockerfile-archlinux-basedevel
    tty: true
    environment:
      - DISPLAY=${HOST_DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
